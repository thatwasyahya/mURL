[package]
name = "murl-daemon"
description = "murl-daemon — a resident mURL resolver providing a persistent consent surface, warm cache, and single-instance activation over a user-private local socket."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[lib]
name = "murl_daemon"
path = "src/lib.rs"

[[bin]]
name = "murl-daemon"
path = "src/main.rs"

[dependencies]
murl-core = { workspace = true }
murl-net = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
clap = { workspace = true }

[lints]
workspace = true
